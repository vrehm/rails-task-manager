<%= form_for @task do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name %>

  <%= f.label :description %>:
  <%= f.text_field :description %>

  <% if params[:action] == 'edit' %>
    <%= f.label :done %>?
    <%= f.check_box :done %>
  <% end %>

  <%= f.submit %>
<% end %>
